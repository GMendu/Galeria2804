@model Galeria2804.Models.Imagem

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Imagem</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="imagemId" />
            <input type="hidden" asp-for="imagemTam" />
            <input type="hidden" asp-for="imagemType" />
            <input type="hidden" asp-for="imagemName" />
            <div>
                <span>Imagem armazenada</span>
                <img src="~/imgs/@Model.imagemName" width="100%"/><br />
                <input type="file" name="arquivo" class="form-control" value="~/imgs/@Model.imagemName" />
            </div>
            <div class="form-group">
                <input asp-for="imagemDescricao" class="form-control" />
                <span asp-validation-for="imagemDescricao" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
